+++
title           = "Fundraising Lead"
date            = 2026-05-01
time_commitment = "Flexible, seasonal"
dbs_required    = false
remote          = true
apply_email     = "volunteers@1stanytown.org.uk"
# role_open = false soft-disables this role: it does NOT appear in the
# listing grid, the homepage banner, or (from SPEC-11) the nav link, and
# its detail page shows a "no longer open" notice. Demonstrates the
# filter without deleting the content. Flip to true to re-open.
# (NB: "published" is reserved by Hugo for a date - hence role_open.)
role_open       = false
draft           = false
+++

Coordinate the Group's fundraising calendar - Christmas Post, the 100
Club, and the seasonal events. (This role is currently filled; the
front-matter `published = false` keeps it off the live listing.)
